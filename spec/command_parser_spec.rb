require 'spec/spec_helper'
require 'lib/connection'
require 'lib/command'
require 'lib/command_parser'


describe "CommandParser" do

  pending "knows about available profiles"
  pending "knows about available commands"

  pending "fails if command file does not exist"
  pending "fails if profile does not exist"
  pending "fails if profile refers to roles file that doesn't exit"
  pending "fails if profile users roles that are not defined in roles file"
end